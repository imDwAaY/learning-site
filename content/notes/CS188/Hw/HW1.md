## Successor( 后继状态 )
Successor(后继状态)指的是从**当前状态**出发，通过执行一次**合法动作**后所到达的**下一个状态**
![[notes/CS188/static/Pasted image 20251027211702.png]]
当前状态由 “位置 (x,y) + 朝向方向” 组成。执行合法动作（要么沿当前方向直线移动任意距离，要么改变朝向）后，会产生新的状态 —— 这些新状态就是当前状态的**后继状态**。
简言之，后继状态的作用是为**搜索算法**（如广度优先、深度优先等）提供 “下一步可以探索的所有可能状态”，从而让算法能够遍历状态空间，找到从初始状态到目标状态的路径。