---
tags:
  - CS188
---

# Ubuntu

由于 **Ubuntu 22.04+ 的新安全机制（PEP 668）** 导致的：  
它禁止直接用 `pip` 修改系统的 Python 环境（以防破坏系统依赖）。
bash代码为:
```shell
source ~/myenv/bin/activate
```

之后进入文件夹即可
# MacBook
bash代码为：
```Shell
conda activate cs188
```
后进入文件夹